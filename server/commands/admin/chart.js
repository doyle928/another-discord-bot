const { request } = require("graphql-request");

exports.run = async (client, message, args) => {
  // NOT NEEDED UNTIL I ADD NIGHTMARE PACKAGE TO GET GRAPH FROM WEBPAGE
  // if (message.author.id == "157673412561469440") {
  // }
};
